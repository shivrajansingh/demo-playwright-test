# Test Configuration Properties

# Browser Configuration
browser.type=chromium
browser.headless=false
browser.slowmo=0
browser.timeout=30000
browser.viewport.width=1920
browser.viewport.height=1080

# Application URLs
base.url.youtube=https://youtube.com

# API Configuration
api.base.url=https://0b93d062-55ba-4f24-9ea8-c409e94c7fc3.mock.pstmn.io
api.timeout=30000
api.retry.count=3
api.retry.interval=1000
api.default.content.type=application/json

# Test Environment
environment=dev

# Screenshot and Video
capture.screenshots=true
capture.videos=false
screenshots.path=test-results/screenshots
videos.path=test-results/videos

# Parallel execution
parallel.tests=true
max.parallel.threads=4

# Retry Configuration
retry.failed.tests=true
max.retry.count=2
